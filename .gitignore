# dependencies
node_modules/
.pnpm-store/

# build output
dist/
.astro/
.svelte-kit/
.wrangler/
.output/

# env / secrets
.env
.env.local
.env.*.local
.dev.vars

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# local state
*.tsbuildinfo

# Claude Code local session state
.claude/settings.local.json

# pnpm temp files (safe to ignore — can appear on interrupted installs)
_tmp_*

# Import pipeline — raw sources (PII-adjacent) and staging artefacts.
# manual-matches.yaml IS committed (it's project logic, not data) — keep it
# tracked so the matching cascade is reproducible.
build/import/sources/
build/import/staging/
build/import/.venv/
build/import/__pycache__/

# pCloud/FUSE temp files
.fuse_hidden*

# wrangler types — regenerable via `pnpm cf-typegen`
apps/web/worker-configuration.d.ts
